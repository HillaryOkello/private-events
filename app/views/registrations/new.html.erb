<%= form_for (@invitation) do |f|%>
  <%= f.label :event_id%>
  <%= f.text_field :event_id%>
  <br>
  <%= collection_select(:registration, :user_id, User.all, :id, :name, prompt: true) %>
  <%= f.submit "Create" %>
<% end %>
